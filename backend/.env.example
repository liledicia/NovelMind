# 复制为 .env 后填入真实值（不要提交 .env 到 git）

# PostgreSQL 连接串（Railway 自动注入，或手动填写）
DATABASE_URL=postgresql://user:password@host:5432/dbname

# 前端部署后的域名，用于 CORS 白名单
# 例：FRONTEND_URL=https://novelmind.vercel.app
FRONTEND_URL=

# 服务端口（Railway 自动注入，本地可不填）
PORT=8000
